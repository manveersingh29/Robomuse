scan_filter_chain:
- name: box_filter
  type: laser_filters/LaserScanRangeFilter
  params:
     use_message_range_limits: true   # if not specified defaults to false
     upper_replacement_value: 5.0     # if not specified defaults to NaN
